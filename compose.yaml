services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    restart: always
    volumes:
      - .:/app
    ports:
      - ${PORT}:3000
    expose:
      - 3000